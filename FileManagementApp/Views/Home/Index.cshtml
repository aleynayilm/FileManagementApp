@{
    ViewData["Title"] = "Ana Sayfa";
}

<div class="container mt-5">
    <div class="text-center">
        <h1 class="mb-3">Dosya Yönetim Sistemi</h1>
        <p class="lead">PDF, PNG ve JPG dosyalarınızı güvenle yükleyin, yönetin ve silin.</p>

        @if (!User.Identity.IsAuthenticated){}
        else
        {
            <div class="mt-4">
                <h5>Hoş geldiniz</h5>
            </div>
        }
    </div>
</div>